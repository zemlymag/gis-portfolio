# 📸 Добавление фото на сайт

## ✅ Что уже сделано

Ваше фото добавлено в Hero-секцию (главный экран) с красивым дизайном:
- 🎨 Круглая форма с градиентным свечением
- ✨ Плавная анимация появления
- 🌟 Зелёная рамка в стиле сайта

---

## 📁 Как добавить фото в проект

### Шаг 1: Поместите фото в папку public

```
gis-portfolio/
├── public/
│   ├── profile-photo.jpg    ← ваше фото
│   └── resume.pdf
├── src/
└── ...
```

**Важно:** Файл должен называться **`profile-photo.jpg`**

### Шаг 2: Готово!

Код уже настроен и будет автоматически отображать фото.

---

## 🎨 Дизайн фото

### Текущий дизайн:
- **Размер:** 160x160 пикселей (круг)
- **Эффект:** Градиентное свечение зелёно-голубое
- **Рамка:** 4px зелёная полупрозрачная
- **Анимация:** Плавное появление с увеличением
- **Тень:** Объёмная зелёная тень

### Расположение:
- Над именем в Hero-секции
- По центру экрана
- Анимируется при загрузке страницы

---

## ✏️ Как изменить дизайн фото

### Изменить размер:

В файле `portfolio.jsx` найдите:
```jsx
className="relative w-40 h-40 rounded-full..."
```

Измените на:
```jsx
className="relative w-32 h-32 rounded-full..."  // меньше
className="relative w-48 h-48 rounded-full..."  // больше
```

### Изменить форму (квадрат вместо круга):

Замените:
```jsx
rounded-full
```

На:
```jsx
rounded-2xl  // скруглённый квадрат
rounded-xl   // немного скруглённый
rounded-none // строгий квадрат
```

### Убрать свечение:

Удалите эту часть:
```jsx
<div className="absolute inset-0 bg-gradient-to-r from-green-400 to-cyan-400 rounded-full blur-xl opacity-30"></div>
```

### Изменить цвет рамки:

Замените:
```jsx
border-green-400/30
```

На:
```jsx
border-cyan-400/30   // голубая
border-blue-400/30   // синяя
border-white/30      // белая
```

---

## 📐 Рекомендации по фото

### Оптимальные параметры:
- **Формат:** JPG или PNG
- **Размер:** 400x400 пикселей (минимум)
- **Вес:** до 500 КБ
- **Качество:** Хорошее освещение, чёткое изображение

### Как подготовить фото:

1. **Обрезать в квадрат** (1:1)
2. **Сжать** через tinypng.com или squoosh.app
3. **Переименовать** в `profile-photo.jpg`
4. **Поместить** в папку `public/`

---

## 🌐 Разные фото для разных секций

### Можно добавить фото в секцию "О себе":

В секции About после текста добавьте:

```jsx
<motion.div
  initial={{ opacity: 0, scale: 0.9 }}
  whileInView={{ opacity: 1, scale: 1 }}
  viewport={{ once: true }}
  className="mt-8"
>
  <img 
    src="/profile-photo.jpg" 
    alt="Евгений Яровой на работе"
    className="w-full h-64 object-cover rounded-2xl shadow-xl"
  />
</motion.div>
```

### Или добавить фоновое фото:

```jsx
<div 
  className="absolute inset-0 opacity-5"
  style={{
    backgroundImage: 'url(/profile-photo.jpg)',
    backgroundSize: 'cover',
    backgroundPosition: 'center'
  }}
/>
```

---

## 🔄 Изменение фото

Чтобы обновить фото:
1. Замените файл `profile-photo.jpg` в папке `public/`
2. Обновите страницу (Ctrl+F5 для полного обновления кэша)

---

## 💡 Дополнительные идеи

### 1. Галерея фото (несколько изображений)

Можно добавить несколько фото с работы:
```
public/
├── profile-photo.jpg
├── work-photo-1.jpg
├── work-photo-2.jpg
└── work-photo-3.jpg
```

### 2. Hover эффект

Добавьте интерактивность:
```jsx
<motion.img 
  whileHover={{ scale: 1.1, rotate: 5 }}
  transition={{ duration: 0.3 }}
  src="/profile-photo.jpg"
  ...
/>
```

### 3. Эффект parallax

Фото следует за курсором:
```jsx
<motion.img 
  whileHover={{ x: 5, y: -5 }}
  ...
/>
```

---

Ваше профессиональное фото теперь на сайте! 🎉
